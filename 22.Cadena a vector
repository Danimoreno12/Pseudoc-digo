Algoritmo sin_titulo
	Escribir "Dime una frase"
	Leer frase
	Dimension vector(Longitud(frase))
	Para i<-1 Hasta Longitud(frase) Hacer
		comparar=SubCadena(frase,i,i)
		vector(i)=comparar
		Escribir vector(comparar)
	Fin Para
	
	
FinAlgoritmo
